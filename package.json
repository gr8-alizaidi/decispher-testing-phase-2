{
  "name": "checkout-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "E-commerce checkout API — Fastify + Drizzle + Postgres",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "vitest run",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "fastify": "^4.28.0",
    "drizzle-orm": "^0.33.0",
    "postgres": "^3.4.4",
    "stripe": "^16.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
