# First Project — Project Context Map

Generated by Decispher. Do not edit.

## Always-true rules

These are CRITICAL invariants. Every AI agent loading this project should treat them as non-negotiable.

- **All money amounts stored as integer minor units (cents), never floats** — Every monetary value in the database, API, and domain layer MUST be represented as an integer count of minor units (e.g. cents for USD) using a bigint/number-as-integer column named with a `_cents` suffix. Floating-point types (float, doubl
- **Realtime socket must use a short-lived one-time ticket, not the session token in the URL** — Open the realtime websocket with a single-use short-lived ticket from the ticket endpoint; never put the long-lived session token in the websocket URL.
- **Strict Prohibition on Storing Raw Payment Card Numbers** — The system must not store raw card numbers (PAN). All payment processing must occur via Stripe tokenization, storing only the Stripe token and the last four digits of the card.
- **WebSocket auth via short-lived ticket, never the long-lived session JWT in the URL** — Clients MUST open the realtime WebSocket using a single-use, 30-second ticket obtained from POST /realtime/ticket (authenticated by the normal session). The long-lived session JWT MUST NOT be placed in the ws:// URL query string or any logg

## Topics (load on demand)

**Local stubs:** `.decispher/topics/<topic>.md` — spine inline, unit ID list.
**MCP tools:** `decispher.list_topics()` · `decispher.get_context_for_topic({ topic })` · `decispher.get_decision({ decisionId })` · `decispher.search_decisions({ query })` · `decispher.check_intent({ description })`

| Topic | Label | Units |
|-------|-------|------:|
| `c/decispher-official-mpkarwbs/architecture` | phase-2 architecture | 8 |
| `c/decispher-official-mpkarwbs/coding-convention` | Coding Convention | 6 |
| `c/decispher-official-mpkarwbs/docs-constraint` | docs-constraint | 4 |
| `mcp` | MCP (Model Context Protocol) | 3 |

## Recent changes

- 2026-05-29 · Realtime socket must use a short-lived one-time ticket, not the session token in the URL
- 2026-05-29 · Realtime presence/typing events broadcast over Redis Pub/Sub, never stored in Postgres
- 2026-05-29 · Document edits use server-authoritative CRDT merge, not last-write-wins
- 2026-05-29 · WebSocket auth via short-lived ticket, never the long-lived session JWT in the URL
- 2026-05-29 · Inventory reservation uses SELECT ... FOR UPDATE, not optimistic retry
- 2026-05-29 · Idempotency-Key required on all checkout/charge POST endpoints
- 2026-05-29 · All money amounts stored as integer minor units (cents), never floats
- 2026-05-29 · Phase 1 vs Phase 2 split: read-side enforcement vs write-side capture (both enforce)
- 2026-05-29 · Production deployment via Docker Compose; Paddle for billing
- 2026-05-29 · Keep the Phase 1 / Phase 2 boundary: capture creates records, Guardian enforces

## Need something not here?

Call `decispher.list_topics()` for the live topic list (may differ from this file if new topics were added since the last sync).

Call `decispher.search_decisions({ query: "your question" })` to search across all context.

**1 unit not yet assigned to a topic** — see `.decispher/unmapped-context.md`.
